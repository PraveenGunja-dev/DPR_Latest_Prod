<?xml version="1.0" encoding="utf-8"?>
<!--
  IIS / Windows App Service configuration for the built SPA.
  Vite copies everything in public/ to dist/, so this ships with the frontend ZIP.

  Why it is needed: the backend SSO callback redirects the browser to
  client-side routes such as /projects#sso_data=... and /access-pending.
  Without the rewrite below IIS looks for a matching file on disk, finds none
  and returns its own 404 page, which breaks the SSO round trip and every
  deep link / page refresh.

  On a Linux App Service this file is ignored; use the startup command
  `pm2 serve /home/site/wwwroot --no-daemon --spa` instead.
-->
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="SPA fallback" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <!-- Serve real files and directories as-is (assets, images, fonts). -->
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="/index.html" />
        </rule>
      </rules>
    </rewrite>
    <staticContent>
      <remove fileExtension=".json" />
      <mimeMap fileExtension=".json" mimeType="application/json" />
      <remove fileExtension=".webp" />
      <mimeMap fileExtension=".webp" mimeType="image/webp" />
      <remove fileExtension=".mp4" />
      <mimeMap fileExtension=".mp4" mimeType="video/mp4" />
      <remove fileExtension=".ttf" />
      <mimeMap fileExtension=".ttf" mimeType="font/ttf" />
      <remove fileExtension=".woff2" />
      <mimeMap fileExtension=".woff2" mimeType="font/woff2" />
    </staticContent>
    <httpProtocol>
      <customHeaders>
        <!-- index.html points at hashed asset names that change every build,
             so the shell must never be served from cache. -->
        <add name="Cache-Control" value="no-cache, no-store, must-revalidate" />
        <!-- Security response headers. The API sets the same set in
             backend-fastapi/app/main.py (security_headers middleware). The SPA
             and the API are separate App Services, so both hosts need them.
             Keep connect-src in step with the backend CSP_EXTRA_ORIGINS setting. -->
        <add name="X-Content-Type-Options" value="nosniff" />
        <add name="X-Frame-Options" value="DENY" />
        <add name="Referrer-Policy" value="strict-origin-when-cross-origin" />
        <add name="Permissions-Policy" value="geolocation=(), microphone=(), camera=()" />
        <add name="Cross-Origin-Opener-Policy" value="same-origin" />
        <add name="Strict-Transport-Security" value="max-age=31536000; includeSubDomains" />
        <!-- Retired from current browsers; 0 is the value that disables it. -->
        <add name="X-XSS-Protection" value="0" />
        <add name="Content-Security-Policy" value="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' https://digitalized-dpr.adani.com https://digitalized-dpr-uat.adani.com https://az10lappdprp02.azurewebsites.net; frame-ancestors 'none'; frame-src 'none'; base-uri 'self'; form-action 'self'; object-src 'none'" />
      </customHeaders>
    </httpProtocol>
    <!-- Do not advertise the host in response headers. -->
    <security>
      <requestFiltering removeServerHeader="true" />
    </security>
  </system.webServer>

  <!-- Hashed build output is immutable, so it is cached aggressively instead. -->
  <location path="assets">
    <system.webServer>
      <httpProtocol>
        <customHeaders>
          <remove name="Cache-Control" />
          <add name="Cache-Control" value="public, max-age=31536000, immutable" />
        </customHeaders>
      </httpProtocol>
    </system.webServer>
  </location>
</configuration>
